{
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3"
  },
  "scripts": {
    "start": "parcel index.html -p 8080",
    "build:css": "parcel build ./light.scss -d . --no-source-maps",
    "build:html": "parcel build index.html --public-url ."
  }
}
